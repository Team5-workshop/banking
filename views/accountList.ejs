<!DOCTYPE html>
<html lang="en" class="h-75" data-bs-theme="auto">
  <%- include('partials/head.html') %>
  <body class="d-flex h-100 text-center text-bg-dark">
    <%- include('partials/backGround.html') %>
    <div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
      <%- include('partials/header.ejs') %>

      <main class="px-3">
        <h1><%= user.user_name %> 고객님의 계좌현황</h1>
        <% if (accounts.length > 0) { %>
        <table class="table table-striped table-dark mt-3">
          <thead>
            <tr>
              <th>Account Number</th>
              <th>Balance</th>
              <th>Account Date</th>
            </tr>
          </thead>
          <tbody>
            <% accounts.forEach(function(account) { %>
            <tr>
              <td><%= account.account_number %></td>
              <td><%= account.balance.toLocaleString() %></td>
              <td><%= account.account_date.toISOString().split('T')[0] %></td>
            </tr>
            <% }) %>
          </tbody>
        </table>
        <% } else { %>
        <p>등록된 계좌가 없습니다. 계좌를 개설 해주세요.</p>
        <% } %>
        <a href="/account/enter" class="btn btn-lg btn-light fw-bold border-white bg-white mt-3">신규 계좌생성</a>
        
      </main>

      <footer class="mt-auto text-white-50">
        <p>
          Cover template for
          <a href="https://getbootstrap.com/" class="text-white">Bootstrap</a>,
          by <a href="https://twitter.com/mdo" class="text-white">@mdo</a>.
        </p>
      </footer>
    </div>
    <%- include('partials/script.html') %>
  </body>
</html>